.tmp*
debug-*.log
# Случайная распаковка JAR в корень (jar xf) — не коммитить
/com/
/org/
/META-INF/
/OSGI-INF/
# Maven/Tycho build cache (не коммитить)
site/target/
plugin/target/
feature/target/
target/target/

# P2 update site (site/features/, plugins/, *.jar) — коммитить для publish.yml

# Локальное зеркало gh-pages (site/sync-deploy.ps1)
/deploy/
/content.xml

# Локальные пути PDE headless (site/scripts/pde-local.properties)
site/scripts/pde-local.properties

# Снапшоты opencode перед правками (генерируются автоматически)
.opencode/snapshots/
