# Фиксированная часть номера версии (единственное место для ручного изменения).
# При сборке PDE/Tycho вместо "qualifier" подставляется метка времени автоматически.
# Пример итоговой версии: 1.0.0.10-202606061430
#
# GitHub Actions:
#   - Тег v1.0.0.10 создаётся один раз при переходе на новую линейку версии.
#   - Повторные сборки: push в master или Actions → Run workflow (ветка master).
#   - При смене линейки: измените comfort.release, создайте тег v<новая_версия>, запушьте.
#
# Локально: Build All в site (Eclipse) или site\build.bat (Maven).
comfort.release=1.0.0.10
