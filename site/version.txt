# Фиксированная часть номера версии (единственное место для ручного изменения).
# При сборке PDE/Tycho вместо "qualifier" подставляется метка времени автоматически.
# Пример итоговой версии: 1.0.0.10-202606061430
#
# Публикация на GitHub Pages:
#   1. Соберите локально (Eclipse Build All в site/ или site\build.bat).
#   2. Закоммитьте site/features/, site/plugins/, site/content.jar, site/artifacts.jar.
#   3. Actions → Publish p2 site → Run workflow.
#
# URL: https://tormozit.github.io/EDT.Comfort/  (composite, все версии)
#      https://tormozit.github.io/EDT.Comfort/<версия>/  (одна версия)
comfort.release=1.0.0.10
