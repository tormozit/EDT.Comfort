# Project rules

Обязательно прочитать и применять правила из [AGENTS.md](AGENTS.md) и всех файлов в `.cursor/rules/*.mdc` (особенно `chat-snapshots.mdc` и `scoped-rollback.mdc` — обязательные снимки исходников перед каждой правкой и правила отката).
