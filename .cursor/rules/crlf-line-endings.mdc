---
description: CRLF на Windows, защита от двойных разделителей и авто-ремонт после завершения задачи
alwaysApply: true
---

# Разделители строк — CRLF (Windows)

Файлы на диске — **CRLF**. В `StrReplace`/`Write` — `\n` между строками (не `\r\n`, не `\n\n` между строками кода).

**Поломка:** «шахматка» пустых строк, `0D 0D 0A` / `\r\n\r\n` — не копировать в патч; сначала ремонт.

**Git hook:** см. `.cursor/scripts/install-git-eol-hook.ps1`, проверка — `check-double-eol.ps1`.

**После завершения задачи** в `plugin/src/**`, `plugin/plugin.xml`, `META-INF/**`:
```powershell
powershell -NoProfile -File "C:\VC\EDT.Comfort\.cursor\scripts\repair-double-eol.ps1" -Path "C:\VC\EDT.Comfort\plugin\src\tormozit\Foo.java"
```
При `REPAIRED` — проверить плотность строк через `Read`. Область также: `.cursor/**`, `launch/**`, `site/**`. `plugin/.gitattributes`: `text=auto`, локально CRLF.
